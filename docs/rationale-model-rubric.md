# Frozen comparison rubric before any paid outcomes

Eight fresh cases, three exact requested model names and source-only/focus input
modes. Alternate case order across models and modes in the frozen operator;
no retries, replacement cases, prompt tuning or discarded failures.

Report mechanical completion, every raw proposed tuple, source identity,
direction, decision-versus-fact role, crossperson/scope violations, and initial
support versus later challenge separately. Receipt binding is not entailment.
No single accuracy score across differently identifiable input contracts. Compare
models within each mode; do not call fewer edges an improvement. Empty output
can be correct abstention or missed positive edges, depending on the case.

Cases3and8 explicitly retain source-only ambiguity. Hidden stored focus must not
be used to fault source-only endpoint identity the model could not observe.
Identical input and output budgets with reasoning none compare requested models within one provider family,
not optimized reasoning budgets. Undated aliases are not immutable weights.
Same-family agent reviews are not independent external human validation.

This manual-ingestion test does not establish automatic capture, answer quality,
MCP host reliability or a trustworthy full decision memory loop. Even a clean
eight-case sample is a diagnostic control, not a public superiority benchmark.
All prior failed experiments remain evidence; these new outcomes do not erase them.

For two-reasons-storage, a price can cease to apply now without having been false
historically: do not erase the original $6 reason. Temporary-lunch focus1 still
mentions both today's choice and unchanged usual preference, so residual multi-claim
ambiguity must be recorded rather than automatically counted as a wrong endpoint.
