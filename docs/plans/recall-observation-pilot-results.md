# Recall-observation pilot: retained result contract

## Scope and fixed points

Documentation-only delivery base: `83a10c3b7664b1f67485a19e7d84d24dcb3041a5`
(`origin/main`, checked 2026-09-22). Worktree
`recall-observation-pilot-results`, branch
`docs/recall-observation-pilot-results`. The running evaluation candidate is
separately frozen at `4bc8892f3699e24191331f75c64483f42f45fde9` (PR201),
not this documentation branch or a claim that its dependency PRs have merged.

The user removed the cumulative request-count approval ceiling while retaining
the same cumulative US$50 ceiling. No ledger reset, refund, failed-case retry,
outcome-driven replacement, release, deployment or historical-result revision
is part of this packet. The six new cases and all inputs were frozen before
the sole live launch. No new paid run is authorized by this result document.

## Acceptance

- **E1 — Preserve the experiment.** Describe the actual one-shot result, including
  incomplete or failed outcomes, using independently reconciled metadata only.
  Preserve the ordered five-field values of the previous 4,849 attempts and
  keep old artifact files byte-identical. Identify
  the exact candidate, source/preparation/scorer/configuration identities,
  answer template, models, six-case selection rule and disjoint exclusions.
  A declared upstream dataset revision is not authenticated corpus provenance.
- **E2 — Honest denominators.** Distinguish fixed six, resolved/unresolved per
  arm and common paired count. Report correct/wrong/unresolved counts and only
  supported accuracy ratios. No pooling with old packets, retroactive rescoring,
  treating unresolved as free successful skips, full-benchmark/competitor claim,
  uncertainty bound presented as a confidence interval, or promotion readiness.
  Per-type observations have one case each and are not generalization evidence.
- **E3 — Explain diagnostic boundaries.** Separate capture admission, map
  visibility, model selection, model ranking, final core return, answer packing,
  and reference-session representation. Counts are not relevant-fact coverage.
  State unavailable, dropped and truncated observations explicitly. Incomplete
  traversal is not proof that money or provider tokens ran out. Observability
  and an evaluation provenance repair are not a semantic retrieval improvement.
  Report only causal conclusions that retained evidence actually establishes.
- **E4 — Reconcile money and failures.** Report before/after cumulative
  reservation and count, this run's reservation, known reported usage, unknown
  cost count, failed/timeout/blocked outcomes and retained global-stop state.
  Unknown usage remains reserved; known reported usage is not a provider invoice
  audit. The same US$50 cap remains in force. A finite monetary-derived request
  guard is implementation accounting, not a new user approval requirement.
- **E5 — Private artifacts stay private.** Publish no source prose, questions,
  references, generated answers, raw provider failures, credentials, local paths,
  source IDs or private artifact contents. Retain safe aggregate/count/hash
  evidence and metadata-only case-type observations. Record limitations and
  independently checked artifact hashes without suggesting hashes authenticate
  truth, external corpus provenance or model execution.
- **E6 — Deliver a focused evidence update.** Allowed edits are this plan,
  `docs/limitations.md`, `ROADMAP.md`, and one
  `docs/evidence/recall-observation-pilot.md` report. Leave README, runtime,
  prompts, guard policy, prior reports, package versions and benchmark artifacts
  unchanged. Link pending-code facts to immutable commits or existing PRs where
  the corresponding files do not exist on this documentation base. Choose the
  next smallest offline reliability investigation from the actual observations;
  do not claim the goal complete merely because an experiment finished.
- **E7 — Verify independently.** Primary directly inspects all changed content,
  reconciles its numbers with reviewed read-only evidence, checks local links,
  and runs generic tests, JSON validation and strict plugin validation on Node
  22.16 and 24.15. No TypeScript gate exists in this JavaScript repository. Commit
  the verified candidate locally; separate non-author Standards and Spec reviews
  inspect the same fixed diff. Resolve findings, repeat affected gates/reviews,
  then push a PR against main and verify exact-head CI and mergeability. No merge.

## Ownership and evidence

Primary owns this contract, frozen-run supervision, source-of-evidence boundaries
and direct acceptance. A bounded worker owns the actual report edits after
results reconcile; separate non-author reviewers own Standards and Spec. Models
and effort are recorded from actual assignments, not inferred from labels.
Verification commands, failures, corrections and final candidate evidence will
be recorded here or on the delivery PR; no result is asserted in advance.

## Implementation and result checkpoint

The bounded documentation worker is `gpt-5.6-sol` at high reasoning effort,
working from fixed base `83a10c3b7664b1f67485a19e7d84d24dcb3041a5`.
The initial draft added the permitted evidence report and updated only this plan,
`docs/limitations.md` and `ROADMAP.md`. After the sole run exited successfully,
the worker verified the authorized metadata-only audit file at SHA-256
`87be8c6c1ac51d7b93b12dea5a4e86d7e754ee083d2ab698ba35a1872b46dcb9`
before reading it. The post-run audit and independent replay audit passed.
The report now records fixed N=6, arm and common denominators, the one incomplete
ingestion, five case-level recall arrays, planned source-prefix truncation
exposure and the reconciled budget checkpoint without publishing private
content.

The run added 1,202 requests and 6,880,740 micro-USD reserved, ending at 6,051
requests and 48,395,560 micro-USD reserved with no new unknown outcome,
unsettled attempt, reported timeout or global halt. The original 4,849 ordered
five-field row values matched their canonical baseline; 52 old control files and
113 old artifact files remained byte-identical. The SQLite database itself
legitimately changed. These are reconciliation facts, not a provider invoice or
quality claim.

Affected links are the new report links from the limitations index and current
roadmap gate, plus immutable external links for PRs #198 and #201 and candidate
commit `4bc8892f3699e24191331f75c64483f42f45fde9`. Local link checking, generic
tests, JSON validation, strict plugin validation, independent Standards/Spec
review, commit, push and CI remain primary-owned final gates. Worker checks on
the final prose passed: `git diff --check`, all local links from the four changed
documents resolve, all 17 expected denominator/accounting fields are present,
and no pending-result placeholder remains in the evidence report. No paid
call or private raw-artifact inspection was part of documentation
implementation.

## Primary acceptance before candidate review

Primary inspected the complete four-file diff, checked the actual contributor
and CI configuration at the documentation base, and personally reran `npm test`,
`npm run validate` and `npm run validate --prefix tools/plugin-validation` on
Node 22.16.0 and 24.15.0 with provider keys unset. Both generic runs passed
106/106 with zero skips; both JSON and strict plugin gates passed. The four
changed documents have 22 resolving local links, no pending-result placeholders
and a clean `git diff --check`. There is no TypeScript gate in this repository.

The independent non-author metadata auditor reran the read-only reconciler after
the sole paid process exited; its 32,057-byte output matched the primary capture
exactly at the result digest above. This did not call a model or rejudge answers.
Primary additionally checked the pinned report digest before projecting latency
and truncation counters, and read their code definitions to distinguish planned
source-prefix exposure from actual packed-receipt associations. An initial local
projection used the wrong aggregate property name (`byArm` instead of `arms`);
it failed without a write or provider call and was corrected before any report
figures were accepted. This was an inspection-script error, not a pilot retry.

The final committed diff will receive separate non-author Standards and Spec
reviews; their fixed-SHA findings, any corrections and exact-head CI are recorded
on the delivery PR. No merge, publication, deployment or further paid run is
included in this result packet.
