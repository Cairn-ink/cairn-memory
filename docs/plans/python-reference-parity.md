# Official reference rendering — offline follow-up

Dependency: PR #183 at `9f4e16dafed843bec850cec009ca0c0c24812411`.
Keep that reviewed candidate unchanged while Claude audits its fairness.

## Acceptance

- RP1: Keep preparation v2 and its model-facing files unchanged. An explicit
  offline Python 3 utility derives official `str(answer)` from original JSON
  bytes, after verifying source, prepared manifest and evaluator artifact
  hashes. Never reconstruct numeric lexical types from JS parsed values.
- RP2: Emit an evaluator-only sidecar bound to source digest, exact manifest
  digest, evaluator digest, selected case IDs and renderer identity. Output
  must be exclusive/private, never overwrite files; diagnostics contain no
  reference text. No network, provider, credentials or upstream SDK execution.
- RP3: A JS loader requires an independently pinned sidecar SHA-256 and exact
  preparation/evaluator binding. Reject missing/duplicate/unexpected IDs,
  reference changes, wrong hashes and malformed records before judge calls.
  Checksums attest reviewed bytes, not that arbitrary untrusted text is true.
- RP4: Scoring accepts only loader-issued, case/evaluator-bound rendering
  capability, not a caller-supplied naked string. Existing no-sidecar behavior
  remains unchanged. Aggregate distinguishes verified Python rendering from
  string-only compatibility; preserves unresolved cases and fixed denominators.
- RP5: Synthetic fixtures independently compare pinned upstream prompt bytes
  for integers/floats (including 3 vs 3.0, negative zero, exponent), integers
  beyond JS safe precision, and arrays with strings/escaping. No references
  enter generation inputs. Preserve old v2 artifacts and legacy tests.
- RP6: Dual Node 22.16/24 tests, generic validation, old and new synthetic
  demos, Python tests, independent Standards/Spec reviews and latest-head CI.
  No core changes, live calls, corpus downloads, publication or deployment.

## Ownership

Sol/high worker owns implementation, fixtures/tests and dedicated API docs.
Primary owns integration, shared changelog/limitations/CI, verification and
delivery. Independent reviewers must not be implementers. A live benchmark
still needs exposure accounting, reviewed transport and cumulative spending
controls; API authorization does not certify those mechanisms.

## Paid-run checkpoint (not an execution grant)

Primary read the existing campaign ledger read-only on 2026-09-18: cumulative
limit US$50; conservative reservations US$28.952; remaining US$21.048;
2,654 of 5,000 requests recorded, zero unsettled, state open. These are
reservation-accounting figures, not provider invoices. Recheck before any
future live call; do not reset or replenish the historical ledger. User has
authorized API use, while the current packet remains offline. A future judge
transport must reserve against the same cumulative allowance before sending.

## Implementation record

Sol/high worker implemented reference utility/loader, scorer integration,
fixtures/tests and API documentation. Primary owns shared docs/CI and reruns.
Reference binding checks exact manifest/evaluator bytes, not history/question
content: those remain the separate prepared-v2 generation loader's obligation.
Independent typed prompt fixtures regenerated byte-for-byte from the pinned
upstream pure Python function; no upstream SDK or paid call executed. Review
and complete dual-runtime results are recorded against the final PR candidate.
