# Fixed-candidate retention result delivery

Base `6b765d0f95beea15759e7ae82969e56ebaac536a`.

1. Record the once-only frozen four-case comparison without changing inputs,
   rubric, requests or responses. Preserve eight answer slots and all eighteen
   HTTP attempts; distinguish transport completion from semantic quality.
2. Report exact source-membership counts, added irrelevant exposure, unchanged
   ordered store/rank-input evidence, rank calls, context size and conservative
   budget changes. Unknown input-count costs are not zero or refunded.
3. Summarize all eight answers against actual supplied sources, including the
   missing weight update and unsupported continuing-weight claim in baseline,
   candidate improvement, attribution/abstention controls and less explicit
   reaffirmed comfort. Retain ambiguity rather than forcing a score.
4. Disclose author/nonblind/same-family reviews, small fixed-selection scope,
   NFKC preflight correction, source-built rather than installed-MCP execution,
   and independent larger-set generation variability. No default promotion or
   general reliability/accuracy claim. Define the next end-to-end validation.
5. Update only current roadmap checkpoint and append a results pointer to the
   original protocol; retain historical evidence. Root generic/JSON/strictplugin
   gates on Node22.16/24, independent exact-commit Standards/Spec reviews and
   all17 CI before merge. No paid run, package release or deployment in this PR.
