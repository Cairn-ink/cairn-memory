# Once-only installed selective-correction diagnostic

Base: `2ad8800c246e8f3cf118b79134f3463ea86c6371`, the combined lifecycle
candidate. The preceding fixture/rubric are frozen inputs, not prompt tuning
material. No production, publication, deployment, GitHub merge or user data.

The owner delegated reliability experiments within the existing cumulative
USD50 campaign. This narrower DRI allocation is at most six HTTP attempts and
30,000 microUSD conservative reservation, not a new allowance or a provider
invoice. No request is permitted until the preflight gates below pass. The
existing immutable method grant may be reused, but no old intent, grant or
ledger checkpoint is reset or overwritten. A new exclusive intent is mandatory.

## Acceptance

- CP1: Add a closed `createRationaleCorrectionAttempt` to the existing serialized
  attempt machinery, fixed to baseline `gpt-4.1-mini-2025-04-14`, `cairn_relate`,
  six requests and 30,000 microUSD. Reuse the existing method/session/shared
  ledger guard, not a second budget engine. Existing factories remain unchanged.
  Rejection, cap exhaustion, pin changes, accounting drift, persistence failure
  and transport failure latch stop; no retry or fallback. Add offline unit tests
  for success, seventh-request rejection, other models/methods, insufficient
  budget, concurrent requests and failures without free reservations.
- CP2: A separately reviewed private operator uses an inspected offline-installed
  artifact's real CLI, adapter and core. For each of the three frozen cases,
  manually admit canonical sources and script the seed graph through installed
  public append-only APIs with a local counter and no HTTP, then close the writer.
  Launch the installed CLI with only explicit rationale-review configuration;
  perform one source-only replacement review using all inspected current refs.
  No extraction, capture, answer/judge, tool-selection model or additional cases.
- CP3: Operator requires a new exclusive durable once-only intent in the existing
  budget directory, exact settled checkpoint/headroom and immutable transitive
  source/operator/fixture/rubric/artifact/policy/grant pins. Validate before every
  reservation and send, and after callbacks. Enforce per-case count then generation
  sequence (at most one of each), fixed model/method and exact source-only payload;
  no seed/oracle/scenario labels, graph, status, namespace or private paths sent.
  Pin the installed runtime against the archive. Test-only transport preload may
  forward only exact provider routes to an authenticated loopback parent guard;
  child never receives the real key. Only primary can load the separately
  authorized OPENAI_API_KEY field, never the whole environment into a shell.
- CP4: Record pre/post source records, all incident and default graph views,
  review envelopes, raw received model outputs and actual guarded transport
  accounting. Preserve malformed/failed/not-run slots and unknown costs; never
  turn failure into empty output or drop inconvenient edges. Close the writer,
  reopen the installed CLI keylessly and prove cold state matches with zero HTTP.
  A structural/semantic rejection may remain recorded while the next independent
  scheduled case runs; any transport, pin, accounting or cleanup uncertainty stops
  further paid work. No retry, replacement case or rerun in another directory.
- CP5: Before paid use, primary and independent reviewers inspect the exact
  candidate and private operator. Fake-HTTP rehearsals on Node22.16/24 exercise
  success, invalid output, unavailable response, exhausted/mismatched budget,
  changed pins, wrong model/method, repeat intent/cross-directory replay, and
  keyless no-HTTP phase. Rehearsal evidence includes installed source hashes and
  durable guard reservations; final all applicable CI must pass. No real key is
  used for rehearsal. Public changes run contributor and live-offline gates.
- CP6: Independently review every source and returned proposal after a single
  live attempt. Separate removal of unsupported challenges, retention of genuine
  challenges, preservation of source-supported reasons, extra unsupported links
  and missing evidence. Do not grade all alternative supported tuples as wrong
  because they differ from scripted controls. Unknown/ambiguous remains unknown.
  The model does not see the old graph; this is fresh inference plus replacement,
  not self-correction reasoning, natural host behavior or general reliability.
- CP7: Publish only an explicit synthetic whitelist projection and honest report,
  with source/artifact/operator/fixture/rubric hashes, all attempted/not-run slots,
  costs/reservations/unknowns, limitations and independent nonblind same-family
  review disclosure. No credential, provider headers or private path/identifier
  leakage. No default model, prompt, automatic capture or semantic promotion.
  Even three passing cases justify only a larger fresh held-out evaluation.

## Model and resource basis

The official model page, checked 2026-09-17, lists the pinned snapshot and text
rates of USD0.40 input / USD1.60 output per million tokens:
https://developers.openai.com/api/docs/models/gpt-4.1-mini
These match the existing guarded policy; no policy or pricing change is made.
Count calls without usage retain unknown cost and full reservation. Availability
for the account has not been probed. No paid connectivity test is allowed.

Read-only campaign checkpoint before preparation: 2,219 requests, 25,172,000
microUSD reserved, 1,371,370 known usage estimate, 1,035 unknown-cost requests,
zero unsettled, state open; cumulative ceiling remains 50,000,000 microUSD and
5,000 requests. Revalidate immediately before dispatch; this snapshot is not
permission to bypass the guard or reserve after another experiment changes it.

One Sol/high worker owns the closed attempt/tests and a bounded private operator
plus rehearsals. Primary owns integration, exact preflight, any authorized live
dispatch, direct raw-source judgment and accounting. Separate non-implementing
reviewers own Standards/Spec and source-semantic review. Agent costs unavailable
from the runtime are not inferred or charged as a reported provider measurement.

## Preparation checkpoint — not permission to dispatch

Primary reran the closed attempt tests (4/4), the full ordinary live-offline
suite, generic tests (114/114), JSON validation and strict plugin validation on
Node22.16.0 and24.15.0: all passed. Installed-only cases intentionally skipped
by the ordinary suite still require the separate private operator rehearsal.
The implementation worker's first operator draft required fuller transitive
input pins, fresh database/sidecar checks and unconditional stopping on cold
read or cleanup uncertainty. These are preflight findings, not model results.
No real key has been loaded and no paid request made for this diagnostic.
