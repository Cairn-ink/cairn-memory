# Fresh paired chronology comparison

Base: `8975237e35282d7163fe3e56858fd4c35e87cbef` (PR144 candidate).
The six histories and source-based rubric in that base were frozen after the
instruction candidate and before any scoring. Do not edit their content or
rerun the preceding three-case diagnostic. This is an experiment preparation,
not a product/default change, and a scripted pass is not semantic evidence.

## Acceptance

- TP1: Add a separate `createTemporalComparisonAttempt` closed cap using the
  existing serialized durable-guard wrapper: exactly 24 maximum HTTP requests,
  120,000 microUSD maximum reservation, 5,000 per request, only the pinned
  `gpt-4.1-mini-2025-04-14` and `cairn_relate`, only count/generation routes.
  Preserve all existing factories and the exhausted correction intent. Require
  full remaining headroom, exact settled checkpoint, pin checks and exclusive
  new intent before dispatch. Never reset, refund or widen the campaign ledger.
- TP2: Private export-only operator compares each of six fresh cases once in
  both arms. Alternate baseline/candidate execution order between cases; each
  arm gets a separate fresh synthetic SQLite database and identical manually
  admitted normalized sources. No rubric, case labels, expected links or old
  graph reaches the model. There is no scripted seed graph: this isolates
  relation inference and persistence, not repair of an existing graph.
- TP3: Use the same installed core and OpenAI adapter for both arms, wrapping
  only candidate `relate` with the frozen temporal facade. Verify complete
  source/runtime/dependency/archive/operator/fixture/rubric/policy/grant pins
  before imports and every paid dispatch, with detached immutable snapshots.
  The parent supplies an explicit transport; no default fetch or key loading.
  Adapter local counting and candidate recounting are local; each arm permits
  one actual provider count then one generation. Enforce exact expected source
  payload, arm system instructions, schema, model, output ceiling and order.
- TP4: Retain all raw responses before consumption and all per-arm statuses,
  before/after records, direct/incident graphs, source-ID mapping and counts.
  Close the writer before a separate keyless child loads the same installed
  core and verifies identity-checked records/graphs. No network/model on cold
  read. Invalid model output remains a failure, never an empty success; later
  independent cases may proceed. Transport, accounting, pin, persistence or
  source/cold mismatch halts and preserves remaining not-run slots. No retry.
- TP5: Offline tests on Node22.16/24 establish caps, rejection/latching, complete
  24-call fake HTTP run, exact modified versus baseline request, nonempty cold
  graph, malformed output preservation, transport stop, replay denial,
  occupied DB protection, pin and caller mutation rejection. Public changes
  pass full ordinary live-offline, generic, JSON/strict plugin checks and two
  independent fixed-candidate reviews plus exact-head CI. Private operator
  receives separate independent review and primary rehearsal before real use.
- TP6: If all gates pass, the primary may allocate this once-only maximum
  US$0.12 reservation inside the user's standing cumulative US$50 resource
  authority, after a fresh read-only checkpoint and explicit pinned intent.
  All twelve arm outcomes stay in the denominator. Independently assess false
  direction, missed challenges, missing distinct support, uncertainty and
  scope mistakes using the frozen rubric. This small adapted synthetic set is
  neither blind accuracy nor proof of natural capture, retrieval, MCP-host
  integration, user benefit or readiness to change defaults.

Primary owns final source judgment, allocation, raw-evidence projection and
acceptance. One Sol/high worker owns the small public cap/test/docs and private
operator/rehearsal. Separate reviewers inspect both without editing. No merge,
publication, deployment, user data, new schema or model migration.

## Offline preparation checkpoint

The separate public cap and four tests pass on Node 22.16.0 and 24.15.0.
Primary independently ran the full ordinary live-offline suite, generic suite
(121 tests), JSON, strict plugin and marketplace checks on both versions: pass.
The private operator's seven fake-HTTP rehearsals pass on both versions,
including the full twelve-arm/24-call installed-core path with nonempty cold
graphs; malformed output with later arms retained; transport rejection,
occupied DB and exclusive-intent replay denial; exact payload checks; caller
grant/pin mutation; received 429 body retention before guard rejection; and
oversized response prompt halt. Each private file is mode 0600. These are
mechanical checks only. No provider or paid request was made. The primary must
independently rerun private rehearsals and review frozen hashes before any
credential or real transport is supplied.
