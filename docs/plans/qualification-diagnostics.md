# Installed qualification diagnostic (S4c)

Base: `7ddf79160684eef19180b3c298145886c8705fdc`. Private dependent work,
same disclosure hold. The frozen six-case pilot is preserved: six failed initial
events, six not-run later events, 24 HTTP200, no semantic accuracy claim.
Do not alter its fixture, operator, prompts, validators or evidence.

## Acceptance

- D1: A separate explicit injected operator runs exactly one new fixed synthetic
  capture through installed MCP, then inspects any successful admissions. New
  source: user, "For my personal reading notes, use short numbered lists."
  Fixed batch ID `qualification-diagnostic-v1`; no pilot cases reused. Imports
  never execute, discover credentials or create budgets. No alternate model,
  semantic judge, retry, correction, trusted binding or retirement.
- D2: Reuse the existing qualification session, proxy, launcher, evidence writer,
  artifact correspondence checker and serialized pilot-attempt controller. Use
  an already provisioned qualification capability, never provision or modify it.
  Exact existing settled/open ledger checkpoint and original phase <=US$50.
  Add a six-HTTP / 30000-microUSD send cap (count+generation included); all errors
  latch through the shared controller. Exclusive ledger-scoped diagnostic intent
  prevents rerun or partial-intent repair via a different output directory.
  The reused controller conservatively requires its existing 100-request/US$1
  headroom at preflight; this diagnostic's actual send ceiling remains six
  requests/US$0.03. Do not widen or duplicate that frozen controller merely to
  accept a smaller remaining balance.
- D3: Freeze operator/helper/fixture (literal in operator), driver MCP lock,
  existing pilot pin set and installed archive. Validate before every request.
  Preserve real key only in parent; private evidence root0700/files0600. Launcher
  diagnosticDirectory uses the existing installed content-free collector.
- D4: Retain capture/inspection envelopes, all finite diagnostic events, exact
  request methods and known usage vs conservative shared reservation deltas.
  For this synthetic diagnostic only, retain bounded request bodies and bounded
  parsed provider JSON via the guarded session response (never HTTP headers or
  credentials). This permits offline diagnosis without rerunning a failed case.
  No raw error message reflection. Every writable failure report is retained;
  missing/incomplete diagnostics must not be treated as proof of a cause.
- D5: Root installed tests with fake upstream inject qualification envelope,
  usage, output-token bounds, JSON and invalid-anchor failures. Prove the actual
  installed collector distinguishes adapter rejection from core invalid
  qualification. Also verify success, transport failure, cap/no retry, duplicate
  intent and key absence from retained artifacts. Existing pilot remains green.
- D6: Root Node22.16/24 full offline-live, artifact, generic/JSON/plugin gates;
  exact final candidate Standards and Spec review before live execution. No
  production, public security disclosure or registry publishing. Source production
  success is not currentness/semantic correctness. Actual diagnostic run is
  limited to the single new case and retained even if it fails.

Exact operator options: `{ledger, expectedCheckpoint, qualificationExtension,
apiKey, fetchImpl, nodePath, cairnExecutable, cairnArtifact,
cairnArtifactSha256, privateDirectory, pins}`. Export pin getter and explicit
`runQualificationDiagnostic`; no implicit execution CLI or environment reads.
