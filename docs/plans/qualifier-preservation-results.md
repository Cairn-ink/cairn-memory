# Qualifier preservation first-attempt report

Base: `0ab03181ae600c0025f9b2465225a31d7cc9ea2c`.
This delivery reports the frozen PR108 diagnostic; it must not change the
fixture, rubric, runtime, prompts, operator or first-attempt evidence.

## Acceptance

1. Record source, fixture, rubric, operator, installed archive and raw-report
   hashes; distinguish installed explicit MCP execution from natural host use.
2. Retain all twelve history and twenty-four answer slots, including failed or
   not-run operations. Separate staged retention, admitted receipts, retrieval
   coverage and semantic answer review. Never use control success to rescue a
   failed memory-system outcome.
3. Inspect commitment amplification, attribution, reason and temporal limits
   against the frozen rubric. Preserve reviewer disagreements and omissions.
   Label six authored pairs, one sample per arm, nonblind same-family review;
   do not claim a general reliability score or a causal retrieval advantage.
4. Record offline success/failure rehearsals, exact once-only request counts,
   cumulative and per-run reservations, known usage estimates, unknown costs
   and unsettled requests. Do not publish keys or raw provider metadata.
5. State an evidence-driven next implementation or experiment gate, with no
   silent retry, source promotion, expanded budget, release or deployment.
6. Run generic tests/JSON validation and documented strict plugin validation.
   Review the fixed candidate independently on Standards and Spec, then pass
   every required CI check before any authorized merge.
