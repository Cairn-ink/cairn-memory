# Installed source-candidate opt-in — Z acceptance contract

Fixed base f4e3666f610b2f728c7c5f87d0bbe99a115570ca (accepted K/#240).
Branch feat/installed-keyset-host-policy in an isolated worktree. One GPT-6
Sol/high worker implements; primary owns decisions and independent acceptance.
Two nonauthors review Standards and Spec on final exact base/candidate.
Copy this contract into docs/plans/installed-keyset-host-policy.md before code.

Outcome: make the already accepted experimental source-candidate policy usable
through installed MCP and Hermes configuration. This is offline product-track
preparation alongside Y, not default promotion, a semantic result, complete S4
growth evidence, or a paid experiment. It does not change the comparison cohort
or frozen method after evaluation; later shared comparison must bind its choice.

## Acceptance Z1–Z10

- Z1 MCP createCairnServer accepts optional own-data
  sourceCandidatePolicy:'bounded-keyset-v1', snapshots it and forwards exactly
  to openMemoryCore. Unknown/null/undefined/wrong type/getter reject before
  opening a database or model work; getter invoked zero times. No new default,
  tool, tool argument, namespace/readset authority, source shape or core change.
- Z2 CLI accepts exactly --source-candidate-policy bounded-keyset-v1. Duplicate,
  missing or other values reject using existing startup semantics. --check-config
  reports sourceCandidatePolicy only when selected, without opening storage or
  contacting a provider. Keyless startup/inspection still work; recall still
  requires the existing model. No environment-variable or installer-default
  setting added. Existing install:preview need not grow another option: document
  adding the explicit startup flag to an installed local executable.
- Z3 Policy is independent of recall-context, capture and source-snapshot.
  Core activates it only for explicit source-evidence/rationale-evidence recall,
  including source-evidence supplied by the existing host preference. Explicit
  contextMode/includeQualification precedence remains unchanged. Default/body
  calls retain old scoring, even when the larger policy was configured. No
  larger token/selection/rank/result budgets and no silent fallback to full scan.
- Z4 Hermes profile/wizard adds optional source_candidate_policy with only the
  exact bounded-keyset-v1 value. Validate at profile load/save and independently
  in bridge, forward fixed CLI arguments for list/call. Missing retains prior
  config behavior; invalid types/values reject before child/database/provider.
  _request's existing immutable saved-profile routing remains; no arbitrary
  caller flag forwarding, tool inventory/timeout/key routing or passive hook.
  Model tool arguments cannot set/override this policy or namespace.
- Z5 Add focused MCP/CLI tests for strict validation/getters0/no DB/model access,
  configuration report omission/presence, unchanged schemas/tools and per-call
  spoof rejection. Compare default/body and both source-context activation,
  context preference plus explicit overrides. Mutating supplied options after
  construction cannot change the core policy.
- Z6 Real installed MCP acceptance: build an inspected local archive and install
  offline using existing packaging helpers. Seed 1,025 distinct memories and
  retained receipts through the INSTALLED public core.admit (not source checkout
  import/raw SQL). Use distinct generic bodies and per-source unique markers.
  After seeding, select a source marker on the actual beyond-prefix maximum-ID
  row to establish the diagnostic. Model callbacks see only query/visible labels,
  never hidden target IDs. Through a cold actual stdio installed CLI, configured
  source recall returns that exact original receipt; default/no-policy and body
  controls retain the known miss. No downloaded corpus/answers or semantic claim.
- Z7 The same installed path must inspect/correct/forget, then restart and prove
  stale old receipt/query cannot return as current; use a changed marker and
  revision, not only checking returned status. Foreign owner/profile isolation
  and keyless inspection remain. Corrected memory source semantics follow
  existing core/host contract; do not change receipts or fake revision history
  to make this test pass. Report any broader preexisting fault before editing it.
- Z8 Actual pinned Hermes MemoryManager/provider with installed archive and
  scripted fake HTTP must exercise profile→bridge→installed MCP→core forwarding.
  Seed synthetic profile through installed core only, bound to its actual owner.
  Native cold-session sourced recall reaches the same >1,024 target, with no
  override in a test wrapper that would bypass the new profile/CLI forwarding.
  Verify schema/wizard/config invalid cases, unchanged five-tool inventory when
  this sole opt-in is used, and separate profile isolation. Actual AIAgent tool
  choice can stay scripted; do not label this semantic or real-user evidence.
- Z9 Narrow docs explain experimental opt-in, larger local authorized source
  examination, literal/first-four/CJK/topK/20k limits, unchanged model exposure
  bounds, source-only context requirement, normal model cost/retention limits,
  and no semantic/SLA/default-promotion claim. Add CHANGELOG entry, not version
  bump/release. No brand redesign; read shared brand ruler before user-facing
  CLI/profile copy. Keep raw synthetic IDs/source/private paths out of public
  aggregate evidence. Existing K core docs saying no host option must be updated
  narrowly on this dependent candidate, preserving historical K's boundary.
- Z10 Both Node22.16/24.15: full MCP, artifact, generic/JSON/pinned strict plugin
  and marketplace, targeted new installed tests. Prepare packaging cache via
  documented metadata-only preparation; do not substitute npmci for it. Run
  all existing five pinned-Hermes native test files plus new focused file through
  its canonical runner, --file-retries0, both installed Node executables. Inspect
  host scripts/README before operational runner and use synthetic profiles only.
  No providers/keys/production. Freeze scoped local commit after gates; primary
  reads actual diff and independently repeats key installed flow on exact SHA;
  two nonauthor fixed-base reviews, fix/retest/rereview, scoped PR main and all
  latest-head CI green/mergeable before ready. No merge/release/deploy.

## Allowed files and next boundary

adapters/mcp/server.mjs and cli.mjs; focused new/extended MCP tests/fixtures;
integrations/hermes/cairn/{__init__,bridge}.py; focused Hermes tests;
packaging/test focused installed acceptance only (no packaging runtime changes);
this plan, narrow standalone-mcp/hermes-memory-provider/source-evidence-context/
protocol/limitations docs, integrations/hermes/cairn/README.md and CHANGELOG.
No core, database schema, defaults, model/adapter wire, ledger, CI, dependencies,
lockfiles, actual user profiles, API keys, benchmark data or old experiment reuse.
If fixture/owner binding requires another file, report exact scope before edit.

This packet proves installed configuration and a diagnostic reachability/lifecycle
flow, not full S4. The frozen 100/1,000/10,000 growth experiment, cost/latency
measurements, general semantic comparison and cold-context onboarding still need
their own evidence. Source authority and remembered-consent boundaries remain.

## Implementation and verification record

Owner: delegated GPT-6 Sol/high implementation, fixed base
`f4e3666f610b2f728c7c5f87d0bbe99a115570ca`; primary independently accepts
and assigns both fixed-base review axes. The frozen contract above was copied
byte-for-byte before code changes. No paid API, actual user profile, private
corpus, operator ledger or old consumed experiment helper is part of this work.

Changed entrypoints and affected callers:

- Programmatic `createCairnServer` validates and snapshots the sole new own-data
  option before `openMemoryCore`; MCP tool schemas and the five-tool default do
  not change. The focused MCP SDK test covers invalid getters, post-constructor
  caller mutation, both source contexts, body/default, startup preference and
  tool-argument spoofing. Existing MCP tests remain the owner for old options.
- `adapters/mcp/cli.mjs` parses the exact optional flag and reports it only in
  syntax-only `--check-config`. The installed archive test uses the actual CLI,
  public installed core for admission, fake HTTP for recall, cold stdio sessions,
  correction/forgetting and foreign-owner isolation. Packaging runtime and
  installer receipt are unchanged; the flag is appended explicitly at startup.
- Hermes `validate_config`, wizard schema and `bridge.exchange` carry one
  profile-bound setting. Existing native provider, agent-loop, qualified and
  capture-recovery test files remain callers; the new pinned-host file covers
  the actual profile→bridge→installed CLI/core path and rejects invalid bridge
  values before an SDK child. No model tool argument or passive hook controls it.
- User-facing boundaries are updated in the narrow MCP, source-context, Hermes,
  protocol and limitations docs plus the changelog. Historical K constructor
  evidence remains attributed to its original stage; this dependent host option
  does not reinterpret the old result.

Retained development evidence: the first focused Node 22 run failed before
tests because the new worktree lacked isolated MCP dependencies
(`ERR_MODULE_NOT_FOUND` for `@modelcontextprotocol/server`); documented local
`npm ci --prefix adapters/mcp` and `npm ci --prefix adapters/openai` resolved
that setup failure without tracked dependency changes. A first docs patch did
not apply due to an exact-context mismatch in `docs/protocol.md`; it made no
file change. The corrected patch is in the current candidate. Neither is a
product behavior failure. Node 22 focused MCP passed 3/3 (including a 1,025-row
actual-core flow); the installed archive test passed 1/1; the new pinned
Hermes canonical runner file passed 2/2 with `--file-retries 0` and scripted
HTTP. These are pre-freeze results and do not replace the full dual-runtime
matrix or final exact-head primary acceptance.

The first full pinned-Hermes six-file Node 22 matrix was **red** (22 passed,
2 failed). Its two old fake-provider fixtures were byte-identical to the fixed
base and returned inline `evidenceIndices`, while the accepted installed
adapter requires provider-only `evidence-pool-v1` with `pool` and
`evidenceSlots`. The installed adapter source hash matched checkout. A
single-file canonical `--file-retries 0` loop reproduced the first failure in
3.4 seconds; retained fake-HTTP method log showed extract count/generation,
then qualification count/generation, and no later method. The old
`test_qualified_conversation.py` and `test_capture_recovery.py` synthetic
responses alone were converted through the existing test-only
`qualificationPoolWire` helper. Both isolated files then passed (1/1 each)
without weakened source, deadline or lifecycle assertions. This is a
preexisting fixture-wire mismatch exposed by the required full gate, not a
new source-policy production regression. The original red result remains in
the private test log; no retry was counted as an unexplained pass.

Final author matrix, serialized with explicit pinned Node paths and no
provider/credential use:

| Gate | Node 22.16.0 | Node 24.15.0 |
| --- | ---: | ---: |
| `npm run test:mcp` | 94/94 | 94/94 |
| `npm run test:artifact` | 85/85 | 85/85 |
| `npm test` | 112/112 | 112/112 |
| `npm run validate` | pass | pass |
| `npm run validate --prefix tools/plugin-validation` (pinned Claude 2.1.260, marketplace and strict plugin) | pass | pass |
| Pinned Hermes canonical six-file runner, `--file-retries 0` | 24/24 | 24/24 |

Setup used `npm ci --prefix adapters/mcp`, `adapters/openai` and
`tools/plugin-validation`, followed by `node packaging/prepare-cache.mjs`
(public metadata only) before artifact tests. The native runner used its
documented six-file command, the separately inspected offline installation
and each exact Node executable; the source archive emits a harmless `.git`
warning during optional precompile. The installed archive SHA-256 is
`f71e099152520feac8efcd0fd022a028feb94fbb2ac3590983d33ce21f864b3c`.
The author matrix and pre-freeze primary synthetic installed-flow check do not
substitute for primary final-SHA acceptance or the two independent review axes.
