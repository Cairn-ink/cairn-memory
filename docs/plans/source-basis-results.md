# Preserve the source-basis comparison evidence

Dependency: aecc90eec9c8f4593bac2031683b1e5db6968ab8. This is an evidence-only
delivery, not a runtime change or permission to repeat an experiment.

## Acceptance

1. Export the exact fixed 48-arm source-basis-comparison-v1 schedule through a
   pure closed projection. Preserve failed/unrun slots, malformed/unavailable
   proposals distinctly from empty, source inputs and proposed units/links or
   edges. Include compiled anchors, source-integrity and cold-replay checks.
2. Retain immutable source/operator/artifact/fixture/rubric hashes and cumulative
   accounting, distinguishing reservations, known usage estimates and unknown
   costs. Do not publish local paths, headers, keys or unrelated metadata.
3. Publish the frozen fixture/rubric and per-case semantic analysis, including
   negative results and ambiguous whole-memory endpoints. Do not convert a
   matching quote, output count or completed call into a semantic success.
4. Explain manual admission, small adapted/nonblind synthetic sample and bundled
   prompt/schema/representation changes; no default or reliability promotion.
5. Offline regression tests pin the public artifact, denominator, failure/null
   behavior, projection privacy and observed adverse outcomes. Run contributor
   and live-evidence offline gates on both Node versions and dual review.
