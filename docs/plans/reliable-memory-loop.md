# Reliable explicit memory loop

Base: 7b18f787ac977bac7240feb04a6bd08270b69a9f (#43 merged).

## Acceptance and execution order

- R1: Run one separately frozen diagnostic trial through actual pinned Hermes,
  installed current core, unchanged baseline/model/prompts/authority-v2, fresh
  synthetic profile, and enabled bounded diagnostics. Retain all stages and
  independent semantic review, including failures. No historical cause inferred.
- R2: Reuse the original USD20 ledger and exact identity; current checkpoint is
  729 requests / USD6.120 reserved / USD0.686775 known usage estimates /339
  unknown-cost requests /0 unsettled. Primary alone loads the authorized existing
  OPENAI_API_KEY in memory, never copies/sources the application environment or
  exposes it to workers/children/logs. First diagnostic trial has an additional
  USD2 reservation stop line and 100-attempt stop line, not a new allowance.
  All HTTP uses existing single-attempt guarded routes. No retry-until-green.
- R3: Reduce any observed failure into a fast offline regression at the actual
  failing seam before changing runtime. Preserve source/revision/namespace
  validation, budgets and no guessed fallback. Rank falsifiable hypotheses and
  change one variable at a time; synthetic reproduction is not historical proof.
- R4: Audit loop acceptance separately. Version any strengthened predicate;
  mechanical negative fixtures must reject false forgetting, duplicate initial
  decisions, missing receipts and stale/foreign revisions. Semantic fixtures
  (negated current facts, fabricated control searches) must require independent
  review and be rejected in that review, not approved from mechanical success.
  Keyword checks alone cannot establish semantic success. Every v3 verdict
  requires review, and failed stages must not advance inspector state. Preserve
  the v2 inspector and explicit v2 runner option for historical comparison; v3
  becomes the default. Do not retroactively apply it as the original outcome.
- R5: After a concrete fix and offline gates, freeze a bounded fresh confirmation
  batch before paid execution: three fresh-profile complete loops, no replacement
  of failed trials. All A-F must pass actual state/tool and independent semantic
  inspection; no active deleted memory, wrong current value or false success.
  Report denominators, latency and usage/reservations separately. Passing a small
  explicit synthetic batch is not general memory quality or human usefulness.
- R6: Use isolated worktree, separate test/implementation workers, primary
  verification on Node22.16/24, installed artifact and actual-host offline gates,
  independent Standards/Spec reviews on committed diff, then PR. No merge,
  publication, deployment, private app work or automatic capture expansion.

If the baseline does not reproduce, do not fabricate a fix: retain it and define
the next fixed stress/negative batch before execution. If remaining funds cannot
cover the next frozen batch, stop paid traffic and continue offline work. Broader
long-history usefulness and PLG adoption remain separate, pending gates.

## Baseline and fixed confirmation batch

The single R1 diagnostic trial completed A-F and control under unchanged v2,
with all seven independently reviewed as semantically accepted. No diagnostic
event occurred; the historical recall failure was not reproduced and no engine
fix is claimed. Its 33 requests added USD0.840 reserved; cumulative checkpoint
is now762 requests /USD6.960 reserved /USD0.709736 known estimates /348 unknown
cost requests /0 unsettled. The stored original evidence remains unchanged.

The mechanical acceptance audit reproduced two failures before the fix:
duplicate active memory at A and `forgotten:false` at E were both accepted.
`node --test evaluation/live/test/lifecycle-acceptance.test.mjs` was run by the
worker and primary: two positive controls passed and those two negatives failed.
The v3 fix changes only evaluation acceptance, not core/MCP/provider behavior.

Before paid confirmation, freeze exactly three fresh-profile trials on v3 with
the unchanged prompts/model/runtime and enabled diagnostics. Run all three
independent trials, retaining any failure; do not replace a failed trial. The
batch has an additional USD4 reservation /180-request stop line under the same
original ledger, not a new allowance. Infrastructure/budget aborts leave remaining
trials not-run. Primary and an independent agent review all actual answers and
tool/store evidence; require18/18 A-F stages and3/3 controls before calling this
small explicit batch passed. Broader reliability is not inferred from that sample.

## Outcome and verification

All three fixed confirmation trials ran; none were replaced. Primary and a
separate agent accepted18/18 product steps. Controls: one accepted, one rejected
for an invented schedule, one inconclusive due ambiguous access wording. The
frozen combined gate is **not passed**. No product recall failure reproduced and
no engine fix is claimed. [Full evidence](../evidence/reliable-memory-loop.md)
records exact hashes, outcomes and the cumulative USD9.530 reservation checkpoint.

Primary ran the final full live suite with all six pinned-Hermes fixture variables
on Node22.16.0 and24.15.0:38/38 passed, no skips. Generic
`npm run test:live-evidence-offline`:30 passed,8 explicit host skips on both.
`npm test`:31/31 and `npm run validate` passed on each runtime. Pinned Claude
2.1.260 marketplace and strict plugin validation passed. No TypeScript gate
exists in this JavaScript repository. Installed archive unchanged from #43;
the actual-host suite rechecks every allowlisted installed source before traffic.

The final fake host no longer carries a memory ID across sessions: C/E must
rediscover the target through actual recall. Successful synthetic loops require
33 guarded requests, including explicit v2 replay and v3 diagnostics off/on.
An E-specific malformed selection test passes A-D, fails E without a forget call,
preserves the exact corrected memory/receipt snapshot, and leaves F not-run.
This locks down the historical failure path's safety behavior, not its unknown
real-provider cause. Additional acceptance tests retain v2 behavior and require
separate semantic review for known misleading/negated answers.

Pricing checked against the official [GPT-4.1 mini model page](https://developers.openai.com/api/docs/models/gpt-4.1-mini)
on 2026-09-11 Taipei: unchanged standard text input0.40/output1.60 USD per million
tokens; the existing policy ignores cache discounts and retains unknown costs.
