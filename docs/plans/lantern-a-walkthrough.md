# Lantern A evidence walkthrough

Fixed base: `3c7b9ee322a52b6af12ac169eddf1e75eea0a1f9`.

## Acceptance

1. Deliver a self-contained Traditional Chinese HTML explanation of the frozen
   r01a case: exact English question with labelled Chinese paraphrase, all15
   admitted user memories, original sources, admitted text and exact navigation
   labels; distinguish the three assistant messages not admitted.
2. Bind each row to its source ID and actual memory ID/revision, actual MOC
   placement, required/explicitly-irrelevant/unlabelled rubric status, both
   selection and final returned status. Preserve actual selector order as an
   available view. Report baseline3/6 and candidate4/6, not the aggregate23/36.
3. Show actual answer text, label-only visibility and complete stored content,
   five L1 groups and zero L2. Explain same selector inputs but differing outputs;
   candidate retention is not full-label expansion and does not prove rank benefit.
4. Separate observed facts from hypotheses. Preserve the clipped reversal,
   visible-but-unselected reaffirmation, and tentative decision. Do not claim
   causal proof or general accuracy. Synthetic data only; no secrets, external
   assets, requests, telemetry, provider calls or altered frozen artifacts.
5. Work offline in a browser. Provide useful filters, readable mobile/desktop
   layout, accessible controls, source/hash provenance and print support.
6. Verify raw-data identities/counts, browser interactions and screenshots at
   desktop/mobile, generic tests/JSON/strict-plugin on Node22.16 and24, then
   independent Spec/Standards review of final commit before PR delivery.
