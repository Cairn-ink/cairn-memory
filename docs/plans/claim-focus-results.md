# Claim-focus ablation evidence

Base `14e20372aca04efea8283ee835d96f0a4dd4bfa9` (#80).

## Acceptance

1. Preserve the complete one-shot 16-arm fixed schedule, including failed/unrun
   arms, raw proposal tuples, warm/cold incident inspections and actual model
   inputs. Closed public export excludes namespaces, private paths, credentials,
   timestamps, transport headers and provider metadata.
2. Freeze the authored fixture and pre-live rubric caveats. Router uncertainty
   was corrected before execution; no outcome-driven relabelling. Shared-receipt
   baseline inputs lack claim identity; separate receipt support from endpoint
   focus correctness. Redundant permitted links are not mandatory answer facts.
3. Compare source-only versus explicit unverified focus in actual installed
   embedded core/adapter, with manual authored ingestion. Disclose the deliberately
   unsupported focus. No automatic capture, retrieval, answerer or MCP-loop quality
   claim; prior rationale semantic failures are not erased by this experiment.
4. Report all observed semantic failures and independent same-family judgments,
   fixed request counts, exact provenance, timing boundary, reservations and
   known/unknown costs. No selective reruns or cost-zero treatment of unknowns.
5. Tests check projection allowlists, frozen identity/denominators, failures and
   evidence metrics. Contributor/live-offline gates on both runtimes, dual review
   and CI before merge. No new model calls or default changes in this report PR.
