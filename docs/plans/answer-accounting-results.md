# Same-source answer accounting results

Base `2f9539e53b737f18fd4a6c59380eb05671aefd92` (merged protocol PR124).
This package records the once-only frozen synthetic host interpretation diagnostic;
it does not change the compiler, source fixtures, runtime or production defaults.

## Acceptance

1. Retain all24 predeclared slots across six histories, two fixed source views
   and two arms, including failed or not-run outputs. Identify exact source,
   fixture, rubric, operator, manifest and raw-report hashes. Preserve prior
   failed experiments, CI attempts and preparation failures without rescoring.
2. Report observed structure, same-source fairness, failures, source/request size,
   token usage, guarded latency, conservative reservation and known/unknown cost
   with explicit denominators. Separate dispatch intent, guarded reservation and
   evidence of actual responses; never turn missing accounting into free usage.
3. Root and two independently working reviewers inspect every source view,
   ordinary answer, accounting record and final prose against the frozen rubric.
   Disclose authorship and nonblind/same-family review. Distinguish missing input
   from misuse of complete evidence, historical from continuing reasons,
   tentative from committed choice, temporary scope from uncertainty, actor from
   adoption, and absent approval from authority. Keep ambiguities visible.
4. State a supported next decision without presenting source-linked structure,
   mock success or a narrow diagnostic as broad memory quality. No automatic
   default promotion, new graph, engine quality claim, host integration, release,
   deployment, upstream listing or human-use result follows from this package.
   Update only the latest roadmap checkpoint and append the frozen protocol's
   execution status; do not rewrite its source-facing design or rubric.
5. Root generic tests, JSON and strict plugin validation on Node22.16 and24;
   independent fixed-point Standards/Spec reviews and all17 CI before merging.
