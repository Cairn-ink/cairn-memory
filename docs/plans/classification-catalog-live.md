# Catalog input probe: pre-call acceptance

Within the owner's new USD50 phase grant, reserve at most USD0.04/8 HTTP requests
for one paired input-path probe. Use the unchanged gpt-4.1-mini-2025-04-14 profile
and each version's actual core/adapter/guard. Baseline is PR63 `5dbf73c`; candidate
is the reviewed catalog-fix commit pinned by the private operator before calls.
No tuning/retry/replacement of a failed arm. Old paid update cases are not rerun.

1. Four independent synthetic SQLite stores: baseline and candidate, each with
   one or101 unfiled memories and zero groups. Selected memory says: "For the
   fictional Seabrook project, use a diagram to explain the release handoff."
   Other records are unrelated numbered inventory notes. Correct explicit
   admission isolates classification from extraction and reconciliation.
2. Fixed order: small baseline, small candidate, large candidate, large baseline.
   Run classifyPlacement once on the selected current revision, record model
   input/output and actual map completeness. If a valid proposal contains a new
   category, apply it once through the existing guarded transaction and cold-read
   its source-bound placement. No answer or paid semantic judge is required.
3. Mechanism acceptance: candidate receives an empty complete topic catalog at
   both sizes, not unrelated inventory bodies; compatible whole-batch proposal
   and apply/cold persistence are retained. Semantic appropriateness of new titles
   is independently judged afterward. A noop remains a noop, not a successful
   category creation. Baseline incomplete input/failure is retained, not repaired.
4. Initialize ONE separate phase ledger capped at50,000,000 microUSD/5,000 requests
   under the new grant. Preserve previous20M ledger/history untouched; do not spend
   its remaining allowance as an additional grant. Freeze phase authorization,
   candidate tree hashes, operator hash, node/policy bindings and exclusive intent.
   Constructor/key access and native I/O only follow reviewed pins and settled
   budget checks. One guarded request at a time;5000 microUSD reservation per
   count/generation call; reject any other method/model/route, unexpected ledger
   activity, missing headroom, pin changes, non2xx, transport or persistence failure.
5. Every arm and request retained including incomplete/not_run; private evidence
   uses exclusive mode0600 durable writes in0700 directories. Never log secrets or
   export local operational paths. No credentials read during preflight. Reuse of
   intent blocks execution. No model retry, refund, fallback, live repair or reset.
6. Offline core/API gates and independent operator/Standards/Spec review before
   calls. Final evidence review and public export after. This is four synthetic
   classification observations, not a general classification-quality benchmark,
   memory-update reliability result or a release/client endorsement.

Pricing verified against the [official GPT-4.1 mini page](https://developers.openai.com/api/docs/models/gpt-4.1-mini):
standard text input/output USD0.40/1.60 per million tokens. Preserve the existing
conservative5000-microUSD count/generation policy; unknown usage is not refunded.
