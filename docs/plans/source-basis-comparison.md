# Bounded source-basis comparison preparation

Dependency: source-basis review candidate 09dee077ebd6c35026ed10274f81783d127bf0af.
This slice prepares a closed experimental transport, not a new default or a
claim that passage-level interpretation is reliable.

## Acceptance

1. A distinct immutable `experiment-basis-models-extension.json` capability
   grants only `cairn_reviewBasis` for the existing baseline, Luna and Sol on
   exact count/generation routes. Reuse existing pinned conservative prices,
   schema validation, reservation/settlement and callback rechecks. Deny host
   completion, other methods/models, bad schema and modified grants before send.
2. Every old capability and session remains unchanged and rejects reviewBasis,
   even after the new grant exists. Neither factory creates an aggregate ledger
   or silently grants access. Grant creation is an explicit operator action.
3. A parent-only basis session requires explicit key, fetch and the new grant.
   The installed consumer receives only a synthetic key through the eventual
   operator. No credentials or actual grant are created by this PR.
4. A fixed comparison attempt permits only relate/reviewBasis and those three
   models, at most 96 HTTP requests and 2,048,000 microUSD reservations, inside
   the existing aggregate cap (maximum US$50). Keep exact checkpoint checking,
   serialized admission, pin checks, persistence-before-send, read-only phase
   denial and permanent halt. No caller-defined limits or retries.
5. Offline tests exercise both successful methods/models, all route/guard
   denials, immutable old grants, insufficient budgets, failure settlement,
   tampering and exact attempt ceilings. Run contributor, adapter, guard and
   live-evidence offline gates on Node22.16 and24, then dual independent review.

## Subsequent frozen experiment (not executed by this PR)

Eight fresh synthetic scenarios × three models × source-only whole-memory and
source-basis arms = 48 slots / 96 HTTP maximum. Freeze fixtures, rubric, source,
artifact, operator and capabilities before any paid call; retain every failed or
unrun slot and never rerun a failed case. Manual admission isolates relation
interpretation, not extraction, retrieval, full MCP use or human value.

Evaluate attribution, non-adoption, distinct premises, changed current
applicability vs historical truth, temporary scope, uncertainty and positive
reaffirmation. Compare the same canonical sources; do not treat more links as
better. Check exact anchors and unchanged storage for the nonpersistent arm;
absence of persisted basis proposals after cold reopen is expected, not failure.
Passing a small synthetic sample will not justify a reliability guarantee,
default promotion, schema migration, publication or deployment.
