# Navigation-label visibility: fixed real-model diagnostic

Base: c0cbc6027316750a73a8a9423e62aeae4104c2f3 (depends on reliable-loop PR44).

The completed three-trial explicit Cairn loop accepted18/18 product stages;
no-memory controls prevented its frozen combined gate from passing. Do not
rewrite that result or rerun it until green.

An independent offline synthetic probe moved the same relevant fact from the
start of a filed memory past the120-code-point navigation label. With one MOC,
two total map items and complete coverage, the relevance-only scripted selector
changed recall1 to recall0, although direct fetch retained the full fact. Primary
reproduced it. This is a documented information-loss risk, not a proven provider
failure, historical LongMemEval cause or violation of current bounded contracts.

## Frozen diagnostic contract

- Q1: No engine/prompt/provider/limit changes before observation. Actual public
  core, SQLite and baseline gpt-4.1-mini-2025-04-14; explicit synthetic admission
  and deterministic filing (no paid extractor). Query asks who owns the fictional
  Juniper migration; the target fact is `Rina owns the Juniper migration.`.
- Q2: Exactly six planned cases, one attempt each: one memory front/back, four
  memories front/back, sixteen memories front/back. In each pair the same factual
  sentences and filler are reordered; target is first admitted but UUID map order
  is recorded, not assumed. Other memories describe distinct fictional projects
  with no Juniper ownership fact. Each case uses a fresh store. Generic MOC title
  `Project notes`; neither title nor generic prefix supplies the answer.
- Q3: Freeze fixtures and source hashes before any call. Run all six cases,
  retain failures/empty recalls; no replacement cases or answer-aware model
  instructions. Direct fetch establishes that the target fact is stored. Record
  candidate visibility, target selection, final target recall, coverage, finite
  diagnostics and resource use separately. Expected fact/target identity belongs
  only to post-result evaluation, not selector/ranker inputs beyond actual store
  content. Not an official benchmark or general accuracy score.
- Q4: Primary alone uses the same original USD20 ledger (862 requests, USD9.530
  reserved before this probe). Additional local stop line USD1 /100 requests,
  no refill, unchanged single-attempt guarded HTTP and unknown-cost accounting.
  Inject only the authorized key in memory; no worker keys or raw provider logs.
- Q5: Before any proposed fix, require an observed failing boundary and a tight
  offline reproduction. Rank falsifiable alternatives: label information loss;
  pagination visibility; model relevance choice; storage/source loss. Existing
  synthetic tests exclude paging/storage for their one-memory contrast, not for
  every real run. No unproved fallback or enlarged resource budget as a fix.
- Q6: Preserve private raw synthetic evidence and publish only bounded summaries
  with exact provenance via reviewed PR. Any engine change gets its own acceptance
  and tests before implementation. No merging, release, deployment, private-app
  work or general product-quality declaration.
