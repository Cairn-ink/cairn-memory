# Reliability work and prospective benchmark expansion

## Authority and objective

The user approved a **cumulative US$100** ceiling on 2026-09-22. This replaces
the earlier US$50 ceiling; it is not an additional US$100. Preserve the existing
campaign ledger, its 6,051 requests and 48,395,560 micro-USD of conservative
reservations. Unknown charges are not free and old failed cases are not retried.
The request-count cap is an operational guard, not a separate user approval cap.
No merge, release, deployment or production-data change is part of this packet.

The product objective remains a lightweight, independently runnable shared
memory core usable by MCP/Hermes, with attributable evidence, qualified claims,
traceable updates and useful cross-session retrieval. Engineering gate counts
are not semantic-quality scores. Public benchmark and real installed-host
acceptance are separate gates.

## Ordered work

1. **Failure localization.** Reproduce extraction-validation rejection paths
   without provider calls. The last failed model output was not retained;
   synthetic reproduction must not be called proof of its historical cause.
   Preserve admission validation, zero partial writes and public error behavior.
2. **Evidence coverage candidate.** Test whether source ranking retains
   complementary evidence rather than choosing one individually similar card.
   Existing source-evidence recall already fetches retained receipts. Do not
   introduce a second memory engine, fabricate missing source tails, force all
   candidates through, or infer adopted decisions from proposals. Freeze the
   original and candidate prompts and a new synthetic rubric before paid calls.
   Scripted tests establish handoff/safety only. The current 800-unit source
   window and incomplete map traversal remain separate limitations.
3. **Explicit monetary extension.** Independently review the operator-only
   same-ledger extension and its crash/race/stale-authority tests before applying
   it. No ordinary run command silently changes spending authority.
4. **Prospective measurement.** Review the integrated candidate, then freeze a
   new answer-blind LongMemEval-S cohort and its budget projection before launch.
   Keep the previous six-case result unchanged and report this as a different
   sample, not a measured improvement over those six questions.

## Prospective cohort policy (not yet selected or launched)

- Source is the existing 500-case s-cleaned artifact, SHA-256
  `d6f21ea9d60a0d56f34a05b609c79c88a451d2ae03597821ea3d5a9678c3a442`.
  The authoritative source hash must be checked against the retained manifest
  before selection; mismatch is a hard stop, never an automatic correction.
- Exclude the 46 previously frozen slots, including unsuccessful/unexecuted
  ones. Exclusion identity and source bytes are verified independently.
- Target **36 fresh questions**, six per official type. Rank by SHA-256 of
  `cairn-reliability-expansion-2026-09-22:` plus source question ID, with ID as
  tie-breaker. Selection may read ID/type only, not questions, answers, reference
  sessions, output quality or abstention labels.
- Form six rounds, each in fixed type order: user, assistant, preference,
  temporal, update, multi-session. Prepare that fixed candidate set privately;
  budget admission takes the longest prefix of **complete six-type rounds**
  within US$42 of conservative projected reservations and the actual remaining
  cumulative allowance. No skipping an expensive candidate for a cheaper one.
  Freeze the resulting N before any provider request. If no round fits, stop
  paid work, retaining preparation evidence. Exclude all 36 selected slots from
  future selection even when the admitted prefix is shorter.
- Retain the existing pinned memory/answer/judge models, answer-v2 boundary,
  source-only provenance checks, scorer, request bounds and timeout isolation.
  Record the integrated commit. The exact new prompt is a changed experimental
  factor; the old six-case percentages are not a paired control for it.
- Up to US$3 may first be reserved for a separately frozen paired synthetic
  source-ranking diagnostic using the same campaign. That is not LongMemEval
  accuracy. Neither ceiling creates additional cumulative allowance.
- Scoring includes fixed-N completion, correct/incorrect/unresolved counts,
  common-arm comparisons and per-type results. Balanced sampling is not the
  original 500-case distribution; do not call its aggregate a full benchmark
  score. Preserve all failures, absent evidence and unknown costs.
- Freeze data/configuration and pass independent review before launch. Credentials
  remain in memory only. No failed-case retry, replacement, invoice assumption,
  cross-session budget refill or expansion beyond US$100.

## Acceptance and next decision

The near-term success criterion is an honest, larger measured sample plus a
causal paired test of the narrow ranking candidate, not a promised percentage.
If paired source coverage regresses, do not promote the candidate as an
improvement; preserve the result and investigate navigation/retention next.
Full 500-case evaluation and a comparable Mem0 OSS run remain later work whose
cost must be projected. US$100 does not imply they fit in this campaign.

Primary owns this plan, integration, accounting and launch acceptance. Separate
Sol/high workers own monetary-extension implementation, extraction diagnostics,
and the bounded source-coverage candidate. Independent non-author Standards and
Spec reviews inspect fixed candidates; primary personally reruns key gates.

## Offline preparation checkpoint

The primary invoked the existing preparer on the fixed 36-candidate roster,
without printing question, answer or history content. The prepared manifest
SHA-256 is `6f2f06eea2d6d51c4d3d6059c535239febb3d8028cd545600a308c089c0911ae`.
The source and prior 46-slot exclusion file were hash-checked first; the latter
has SHA-256 `2cdec8a1f393d21c431edfe0b0eb1029e877fdf9c1ad9cac922482ad07f10a91`.
No provider call, ledger change, capability claim or paid launch occurred.

The subsequent ordinary loader rejected `invalid_manifest`: it has a legacy
hardcoded seven-case ceiling. The prepared files remain unchanged. This is a
newly observed evaluation-loader limitation, not an ingestion/model failure or
a result for any new question. Add an explicit bounded larger-cohort loader
option with negative/boundary tests; do not bypass digest/source-map checks or
silently treat the failed load as a successful preflight.

A separate read-only projection over the hash-checked prepared history gives
complete-round reservation bounds of 7,141,960; 7,501,960; 7,061,960; 7,021,960;
7,061,960; and 7,381,960 micro-USD. Under the frozen US$42 rule, the prospective
launch prefix is **30 questions**, five per type, with **6,236 requests** and
**35,789,800 micro-USD** conservatively projected. This projection is not the
loader's acceptance, a paid result or permission to launch. Independent roster
review, the repaired loader, the budget extension and integrated verification
remain required before execution.

## Integration ownership and verification

The primary combined four isolated worker candidates on base
`4bc8892f3699e24191331f75c64483f42f45fde9`: extraction diagnostics
`3a7cb44936517e544704a3b03a8f2743460b5faf`, explicit budget extension
`88b57663562d8b7f27362f737fb95642c9e98e04`, source-coverage ranking
`1174206acda370e184984a280f03b6751f7ae414`, and explicit cohort bound
`4c23a75d89d975a13ccc249dc1cc29218701e1df`. Each implementation worker
used Sol/high; actual worker token cost is unavailable.

Primary takeover is limited to integration: preserving both adjacent CLI flags
and changelog entries during cherry-pick conflicts, and adding the new budget
extension tests to the existing request-guard test script used by CI. The
standalone test script remains available. No new runtime behavior is introduced
by those conflict resolutions. The final combined candidate needs both Node
22.16/24 checks and independent non-author Standards/Spec review before paid use.
Component reviews are not a substitute for this combined review.
