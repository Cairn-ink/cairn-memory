# Fixed relation-model comparison attempt

Base 2642297cbcba8818c8f7c3feba91b49e32cc442b (#83 merge).

## Acceptance

1. A named factory adds an immutable 96-HTTP / 2,048,000-microUSD local cap inside
   the existing at-most-US$50 ledger. The planned balanced design is eight fresh
   cases × three models × two input modes × count/generation. This is a maximum,
   not authority to rerun failures or start an operator.
2. Only relate, the three exact model names, and Responses count/generation routes
   are accepted. Per-request reservations are baseline5000/Luna3000/Sol56000.
   Old factories retain exact limits and baseline-only model acceptance.
3. Existing serialized queue, pin/persistence checks, before-send local reservation,
   settled shared-ledger delta verification, permanent halt and readonly gates
   remain. No retry/refund. Require full conservative headroom before construction.
4. A parent-only explicit session uses the new immutable grant and closed models,
   no discovered credentials or default transport. It never creates a ledger or
   authorization. Actual durable guard remains the enforced HTTP boundary.
5. Synthetic tests verify exact balanced cap, expensive-model skew, denied old/new
   routes, wrong accounting, interrupted sends and no implicit grants. Run generic,
   live-offline and guard gates on both Node runtimes and independent dual review.
   No paid run, frozen fixture or artifact/operator claim in this slice.
