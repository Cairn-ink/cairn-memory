# Ordinary-MOC small-candidate retention results

Status: once-only comparison completed; results recorded without default promotion.
Fixed delivery base: `fc62559b4c0ecb3e4fe4e981dd66e508054a291c`.
Frozen experimental source: `a4d7571956db7394d179cd42375a4fca93c55c10`.
Protocol: [ordinary-MOC comparison](../retention-moc-comparison.md).

## Acceptance

1. Publish the once-only outcome only after final root dual-runtime rehearsal,
   independent operator reviews, unchanged authority/budget checks and all17
   source-PR CI checks.
   Preserve raw evidence hashes, actual request totals, all22 answer slots and
   failures; do not retry, regenerate or replace failed answers with controls.
2. Distinguish staged sources, actual qualified admissions, visible navigation,
   selected/fetched candidates, ranked final evidence and ordinary answers.
   Required and explicitly irrelevant source coverage use the frozen rubric;
   unobserved stages and unlabelled sources must not acquire invented labels.
3. Report exact ordered paired rank-input equality and policy applicability.
   Independent selection differences remain end-to-end observations, not an
   isolated rank-policy effect. No forced admissions or small candidate sets.
4. Root and independent content reviews examine every available regular,
   canonical-control and negative answer for qualifier strength, changed and
   reaffirmed reasons, actor ownership, adoption, temporal scope, unsupported
   causality and invented authorization/denial. Cite actual source and answer
   evidence; do not equate valid structure or retained IDs with accuracy.
5. Report context bytes/tokens, provider calls, observed latency and conservative
   reserved cost separately from known usage estimates and unknown-cost counts.
   Keep the existing cumulative US$50 ledger, with no replenishment or new grant.
6. Update protocol result pointers and the delivery checkpoint without erasing
   historical failures. Disclose authored synthetic cases, nonblind same-family
   review and author overlap. Source-built experimental stdio is not installed
   package, natural host-client or real-user proof. No default promotion,
   release, deployment or private integration is included in this documentation.
7. Run generic contributor gates on Node22.16 and24, obtain independent Standards
   and Spec reviews on the final committed diff, and require all17 CI before a
   normal authorized merge. Keep primary main and unrelated work unchanged.
