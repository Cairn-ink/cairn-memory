# Source-addressed development evidence

Fixed base: `c39797515c1ae678c6be54cec72b97f21f60473a` (PR93 merged).

## Acceptance

- Preserve the once-only source-addressed-ablation-v1 fixture and rubric and all
  48 scheduled outcomes, including failed and unrun arms. Do not rerun cases.
- Export a fixed synthetic allowlisted projection retaining original sources,
  numbered source parts, raw proposed ranges and context, compiled anchors,
  roles/links, exact model names, errors, duration and warm/cold preservation.
  Exclude private paths, credentials, provider headers and client/session IDs;
  reject sensitive retained data and wrong experiment/fixture/schedule.
- Keep missing/malformed responses distinct from valid empty output. Preserve
  rejected ranges exactly, not repaired or silently replaced by compiler output.
- Independently compare public projection with private raw evidence. Test its
  frozen hash, every accepted source anchor and all request/reservation totals.
- Separate structural completion from semantic correctness, omissions and
  source-context usefulness. Review all arms against the frozen rubric. Retain
  pre-live clarification: temporary renovation-week quiet applicability may be
  challenged, without permanently invalidating the usual home routine.
- Label adapted development cases, manual source admission, nonblind judgments
  and bundled changes. No full capture/MOC recall/answer or held-out quality claim,
  no default promotion, no inference that exact ranges establish semantic truth.
- Preserve earlier zero-request operator setup failure and corrected offline
  success/malformed/transport evidence in the report; no product-rule relaxation.
- Run live-evidence offline tests on Node22.16 and24, generic/JSON/strict plugin
  checks, and independent Standards and Spec reviews before delivery.
