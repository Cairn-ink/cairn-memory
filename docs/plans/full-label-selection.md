# Full admitted-label selection diagnostic

Status: offline capability; no default or paid execution authorized by this file.
Fixed delivery base: `88938dc639ebe844a44ccaeb34746d5b03c3374a`.

## Question

The ordinary-MOC retention comparison lost required evidence before ranking.
Some decisive clauses were clipped; others were visible but unselected. Test
exposure separately from selection judgment, without treating admitted text as
canonical evidence or full text as an automatic improvement.

## Acceptance

1. Add only an explicitly constructed evaluation model wrapper at the existing
   select port. No production defaults, capture, storage, ranking, prompt,
   transport authority or package exports change. Other model ports stay intact.
2. Bind an immutable explicit read set and public same-store `get` callback.
   For every visible memory reference, resolve active admitted content matching
   namespace, ID and revision. Replace only its label with full admitted content;
   preserve maps, order, references, groups, query, maxRefs and instructions.
   Do not forward receipts or metadata, add references or consult a rubric.
3. Reject malformed inputs, missing/stale/deleted/foreign references and any
   exposed-content changes across callbacks. Check all exposed records, including
   unselected ones, after counters/model callbacks and before return. Keep core
   final freshness checks authoritative; do not claim an atomic snapshot.
4. Detach and freeze model-facing data. Preserve cancellation and provider
   failures without retries. Independently enforce the existing 6,000-token
   whole-request and 1,024-token output limits and bounded input bytes. Oversize
   fails closed; no truncation, selective expansion, padding or fallback. Validate
   output references against exactly the original visible identities and caps.
5. Offline synthetic tests cover unfiled and filed references, namespace binding,
   duplicate visibility, groups, unchanged order/ports, complete Unicode content,
   missing/stale/foreign records, correction/forget during callbacks, cancellation,
   malformed input/output, byte/token boundaries and no-retry failure. Include
   real-core temporary SQLite integration on Node22.16 and24; pure tests also run
   under the generic Node20 gate. No credentials or network calls in tests.
6. Document that full admitted text exposes more potentially irrelevant content,
   may contain extraction errors, and is not a new correctness/privacy guarantee.
   A future once-only paid comparison requires fresh synthetic cases, frozen
   rubric/actual store/operator, existing shared ledger and separate review. Never
   replay scored failures. Hold all variables except labels fixed, report required
   selections/losses/distractors/negative queries/cost/overflow, and reject promotion
   absent repeated narrow gains without regressions. Do not repeat this wrapper
   hypothesis indefinitely or conflate selection with answer-host fidelity.
7. Root verification: generic tests, JSON and strict plugin validation on both
   core runtimes, focused real-core tests; independent Spec and Standards review
   of final committed diff; all17 CI before authorized merge. Preserve primary
   main, frozen evidence and unrelated work.
