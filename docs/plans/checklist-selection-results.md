# Retain the fresh selection comparison outcome

Base: `fbf969947f76bf003bfff1a2028c2c22e8d7920a`.

Document the once-only comparison frozen by PR117. This delivery records
observations; it cannot change sources, rubric, operator, answers or defaults.

## Acceptance

1. Bind the report to its source HEAD, fixture/rubric, operator/launcher/helper,
   raw-report hashes and separately scoped grant. State the source-based
   experimental stdio boundary, shared stores, alternate arm order and unchanged
   baseline/control driver. Do not claim installed CLI or natural-host success.
2. Retain all six histories, eighteen capture windows and eighteen answer slots,
   including failures and not-run entries. Separate admitted evidence, baseline
   recall, candidate recall and answer fidelity; report required/qualifier and
   predefined irrelevant coverage without calling source presence semantic truth.
3. Apply the frozen promotion rule honestly: identify every required source
   retained by baseline but lost by candidate, whether premise coverage improved
   within unchanged bounds, and unsupported assertions or indiscriminate
   abstention. Root plus two non-blind independent agent reviews assess answers;
   disclose the review limitations and preserve disagreements. No general
   accuracy, competitor superiority or external benchmark claim.
4. Localize omissions only from exact observed stage traces. Distinguish raw
   selection proposals from accepted selections, and invalid output from missing
   source evidence. Report input bytes, provider token counts and guarded latency
   separately; operator overhead is not a product performance benchmark.
5. Record six offline rehearsals, request/reservation/known/unknown-cost deltas,
   conservative cumulative headroom, unsettled attempts and cleanup outcomes.
   Never reset or refund the campaign, hide a failed attempt or rerun the fixed
   scored experiment. Publish no credentials, user conversations or private
   operational details.
6. Update the protocol entrypoint and current roadmap with the actual decision
   and next evidence-backed package. Run generic, JSON and strict plugin gates
   on Node22.16/24; obtain fixed-commit Standards/Spec review and all required CI
   before merge. No release, deployment, private integration or default change.
