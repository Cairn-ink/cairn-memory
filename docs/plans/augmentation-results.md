# Bounded augmentation results delivery

Base: `97b09f4afb7476729e602a0afca4fc5a88c2c0e6` (merged comparison protocol).
The frozen experiment source stays at `94e77a6f13fb0774cdae194072929bfc1010f433`.

## Acceptance

1. Publish a truthful `docs/augmentation-results.md` from the once-only frozen
   report, with experiment identity, reviewed source/operator/fixture/rubric and
   raw report hashes, resource limits and actual accounting. Never edit the raw
   evidence or old experiments, retry a failed case or report planned outcomes.
2. Retain all six histories, eighteen capture windows and eighteen answer slots,
   including failures/not-run. Separate stored source coverage, visibility, raw
   selector seed, bounded augmented candidates, ranking and final receipts.
   Explain gains and losses per history, original-choice displacement and
   predefined irrelevant exposure versus final context. Unknown stages stay
   unknown, not fabricated zeroes or inferred selection failures.
3. Root and two separately reviewing non-author-of-answers agents inspect every
   actual answer and supplied source set against the frozen rubric. Record
   decision strength, reasons, actors, scope, unsupported certainty and omissions;
   preserve disagreement and disclose nonblind/same-family and fixture/operator
   authorship. Exact quotes/source IDs and controls are not semantic scores.
4. Report source bytes, model usage, latency, requests, conservative reservation,
   known estimated costs, unknown usage and unsettled attempts independently.
   Source-based experimental MCP is not installed CLI or natural host evidence.
   Six fake-service rehearsals and offline tests are not model-quality proof.
5. Apply the frozen promotion rule honestly; do not hide old full-source errors
   or generalize three authored pairs to production accuracy. Link the results
   from the frozen protocol without rewriting its rules; update the roadmap's
   current checkpoint and next evidence-based step. No default switch, package
   release, deployment, real-ledger/grant change or model rerun in this package.
6. Root generic, JSON and strict plugin checks on Node22.16/24, fixed-commit
   independent Standards/Spec review, then required CI before merge. Protect
   unrelated work and leave the primary worktree unchanged.
