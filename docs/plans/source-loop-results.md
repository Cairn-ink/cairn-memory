# Installed capture-to-basis development evidence

Fixed base: `0edc3e345d736ed823bef791932eb72df0b85d21` (PR95 merged).

## Acceptance

- Preserve the once-only source-loop-controls-v1 fixture, rubric, all four cases
  and all scheduled MOC/lexical/captured-source controls; no live re-execution.
- Pure fixed-experiment projection retains capture status, admitted contents,
  retained receipts, restart parity, actual recall, selected refs and source
  coverage, raw model proposals and compiled basis results. Preserve errors,
  missing evidence, over-cap controls and failed transport. Exclude credentials,
  client/session metadata, private filesystem paths and provider headers.
- Keep original source messages and required source IDs evaluation-only. Public
  fixture/source IDs are synthetic, not customer metadata. Independently verify
  raw/public fidelity and frozen output hash in offline tests.
- Evaluate actual capture/recall/basis outcomes separately. Coverage is source
  presence, not semantic accuracy. Read every model outcome against the frozen
  rubric and retain future applicability of pickup-hours (starts next week).
- Mark adapted development cases, nonblind agent judgments, model variance even
  on equal refs, missing final-answer/MCP/ordered-reconciliation/user validation.
  Preserve negative results without default promotion or misleading success rates.
- Report durable cumulative reservations, known estimates, unknown costs and
  unsettled requests separately. Run dual-runtime live-evidence offline suite,
  generic/JSON/strict plugin checks and independent double review before delivery.
