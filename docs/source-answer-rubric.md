# Frozen downstream answer utility rubric

Sixteen once-only calls: four existing development questions × none, actual MOC
source receipts, lexical source receipts, MOC receipts plus unassessed MOC basis.
Use pinned gpt-4.1-mini-2025-04-14 host completion with the same explicit safe
consumer instruction for every arm. This is a controlled downstream consumer,
not default ChatGPT/Claude/Codex behavior or a tested GUI/MCP host. No tools or
execution actions. Rotate arm order by case. No retries, case replacement,
model switching or tuning after outcomes.

Sources come only from the frozen installed loop raw report. MOC sources preserve
actual returned receipt order; lexical sources derive from the existing refs and
final captured snapshot. No rubric/source-oracle rescue. Graph is explicitly
unassessed. Required-source IDs and expected answers are not in model inputs.

Judge separately: supported useful answer, appropriately unknown/abstaining,
unsupported addition or contradiction, missing requested detail, and whether
graph omission actually harmed the answer. No-memory abstention is appropriate,
not a false answer. Compare supported task completion, not a misleading accuracy
score that punishes correct abstention or treats it as recalled knowledge.

1. Rill: removable battery and rear rack were reasons; sealed replacement changes
   battery removability only; rack unchanged, no different bike selected.
2. Pickup: original17:00 because centre18:00; next week centre16:00 therefore new
   pickup15:00. Old time historically correct; new time not already effective
   today. Explain reason for new arrangement even if graph omits that support.
3. Reading: usual library because quiet; only two noisy renovation days café;
   returns to library after renovation, not permanent. No fabricated dates.
4. Journal: decision based on attributed local-only belief; default cloud sync
   corrected that mistaken belief. No Loom switch adopted or diary move authorized.

All sources and previous graph outcomes are already seen development material.
Judgments are nonblind agent reviews; one response per arm cannot isolate model
variance or establish general reliability. Report all16 answers including failures,
truncation and refusals. This is not a blind benchmark or independent-human review.
Maximum16 HTTP/US$0.80 conservative reservations inside the unchanged50USD ledger;
durable intent, exact pins, no transport retry and permanent halt required.
